# Weekly PRs for the actions in .github/workflows and the two devDependencies.
# Bun's version is pinned in the workflows and moves by hand.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: bun
    directory: /
    schedule:
      interval: weekly
